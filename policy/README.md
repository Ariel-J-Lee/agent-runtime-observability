# Policy

> **v0 PLACEHOLDER.** This directory's real content arrives at Tier 4 in a future implementation packet. The empty state is intentional and is not evidence.

The v1 release will populate this directory with:

- `policy/v1.yaml` — the canonical declarative policy spec for the v1 demo

No policy spec is committed at v0. The recommended language is YAML; alternatives (Cedar, OPA / Rego) may be considered by the implementation packet.
