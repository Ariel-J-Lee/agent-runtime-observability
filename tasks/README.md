# Tasks

> **v0 PLACEHOLDER.** This directory's real content arrives at Tier 4 in a future implementation packet. The empty state is intentional and is not evidence.

The v1 release will populate this directory with:

- `tasks/canonical/v1.json` — the canonical demo task definition
- `tasks/policy_gates/pg{1-5}_*.json` — adversarial fixtures that trigger the five policy-gate scenarios
- `tasks/failure_modes/<mode>.json` — fixtures that trigger each catalogued failure mode

No fixture files are committed at v0. Adding fixtures before Tier 4 is out of scope.
