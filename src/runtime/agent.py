"""Single-agent governed runtime entry point.

v0 PLACEHOLDER. Real implementation arrives at Tier 4 in a future
implementation packet. This module exists only as a scaffold so the
project structure is visible. Importing this module succeeds; calling
into the runtime is not implemented at v0.

See `docs/runtime-model.md` for the full v1 contract.
"""
