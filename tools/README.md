# Tools

> **v0 PLACEHOLDER.** This directory's real content arrives at Tier 4 in a future implementation packet. The empty state is intentional and is not evidence.

The v1 release will populate this directory with tool implementations whose JSON-schema input/output contracts the policy layer checks before execution. The exact tool set is an implementation-packet decision.

No tool implementations are committed at v0.
